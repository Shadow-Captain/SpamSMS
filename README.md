# SpamSMS

```
Instalacion

$ `pkg install python`
$ `pkg install git`
$ `python3 -m pip install requests`
$ `git clone https://github.com/Shadow-Captain/SpamSMS`
$ `cd SpamWa-1`
$ `python3 spam.py`
```